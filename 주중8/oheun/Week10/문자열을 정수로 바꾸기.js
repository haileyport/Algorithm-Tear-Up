function solution(s) {
    let [sign, ...remain] = s;
    // return s.concat([...s].map(Number));
    // 부호로 시작하면
    let answer = [];
    if(isNaN(sign)){
        remain = remain.map(Number);
        return sign.concat(remain);
    }else{
        answer = [...s].map(Number);
        return answer;
    }
    
    
//     let answer = isNaN(sign)? sign.concat(remain):s;
//     return answer;
}

// 아니 이렇게 쉬운거였다고??????????????????????????????????????????????????????????그럴리 없는데 ㅇ0ㅇ
function solution(s) {
    return parseInt(s);
}